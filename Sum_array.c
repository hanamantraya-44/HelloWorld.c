#include<stdio.h>
int main(){
    int n, are[n], sum=0, i;
   printf ("enter the value of n:");
   scanf("%d",&n);
   printf ("enter the elements\n");
   for(i=0;i<n;i++){
       scanf("%d",&are[n]);
   sum+=are[n];
   }
   printf("%d",sum);
}
